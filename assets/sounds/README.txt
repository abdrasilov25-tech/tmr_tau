Place short UI sound files here (<300ms, MP3 or OGG):

  success.mp3         — played on success actions (subscribe, purchase)
  error.mp3           — played on error actions
  message_sent.mp3    — whoosh sound when sending a message
  message_received.mp3 — pop sound when receiving a message

Haptic feedback works without any files.
Button tap uses SystemSound.click (no file required).
Missing files are silently skipped.
